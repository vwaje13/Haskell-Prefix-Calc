The files included in this folder are this folder, exp.txt and a pdf document that explains my approach for the project. To run this project, compile it using the ghc compiler in ta shell and then run the executable. It will prompt the user for a prefix expression, and it will give the user the answer as well as a list of the history. You must use spaces between each token you use. 